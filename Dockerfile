# This docker image is made so one can develop
# this project inside a container, instead
# of installing a local environment

# FROM node:18.14-alpine
# COPY . /home/app
# WORKDIR /home/app
# RUN npm install
# CMD npm start